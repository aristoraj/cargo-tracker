{
  "name": "cargo-tracker-api",
  "version": "1.0.0",
  "private": true,
  "description": "Proxy service between the cargo scanner widget and Zoho Creator. Holds Zoho OAuth credentials server-side; the widget never sees them.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  }
}
